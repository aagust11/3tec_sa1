export function guidanceHTML(guide,esc){return `<section class="idevice idevice-practice" data-export-section="Raonament guiat · ${esc(guide.title)}"><header><span class="device-kind">1. Abans de tocar els controls</span><h2>${esc(guide.title)}</h2></header><div class="device-body"><div class="concept-grid"><div><h3>Situació A</h3><p>${esc(guide.a)}</p></div><div><h3>Situació B</h3><p>${esc(guide.b)}</p></div></div><p><strong>Què mantenim igual?</strong> ${esc(guide.keep)}</p><p>Llegeix A i B. Respon un pas cada vegada. Pots triar «encara no ho sé», comentar-ho amb una parella i després escriure paraules clau. La previsió no rep una nota: la contrastaràs amb les proves.</p>${guide.questions.map(q=>`<label class="note-label" for="guide-${q.id}">${esc(q.label)}</label><select id="guide-${q.id}" data-guide="${q.id}" style="width:100%;max-width:100%;padding:10px"><option value="">Tria una opció</option>${q.options.map(o=>`<option>${esc(o)}</option>`).join('')}</select>`).join('')}<label class="note-label" for="prediction">${esc(guide.prediction)}</label><p id="prediction-support">${esc(guide.starter)}</p><textarea id="prediction" rows="3" aria-describedby="prediction-support"></textarea><p class="save-status" id="guide-status" role="status"></p><h3>Ara comprova la previsió</h3><ol><li>Prem «Prepara A», observa el resultat i prem «Registra aquesta prova».</li><li>Prem «Prepara B» i torna a registrar. Compara les dues files.</li><li>Torna a les teves previsions i completa la conclusió amb les dades. No cal que la primera idea fos correcta.</li></ol><div class="toolbar"><button class="btn secondary" data-prepare="A">Prepara A al simulador</button><button class="btn secondary" data-prepare="B">Prepara B al simulador</button></div><p id="prepare-status" role="status"></p></div></section>`;}
export function guidanceReport(guide,journal){return guide.questions.filter(q=>journal.guidance?.[q.id]).flatMap(q=>[q.label,journal.guidance[q.id]]);}
export function bindGuidance(main,guide,journal,persist){
 journal.guidance??={};main.querySelectorAll('[data-guide]').forEach(el=>{el.value=journal.guidance[el.dataset.guide]||'';el.onchange=()=>{journal.guidance[el.dataset.guide]=el.value;persist();main.querySelector('#guide-status').textContent=main.querySelector('#experiment-save').textContent;};});
 main.querySelectorAll('[data-prepare]').forEach(button=>button.onclick=()=>{const stage=button.dataset.prepare;const values=stage==='A'?guide.controlsA:guide.controlsB;for(const [id,value] of Object.entries(values)){const input=main.querySelector('#experiment-model #'+id);if(!input)continue;if(input.type==='checkbox')input.checked=value;else input.value=String(value);input.dispatchEvent(new Event('input',{bubbles:true}));}main.querySelector('#prepare-status').textContent=`Situació ${stage} preparada. Observa el resultat i prem «Registra aquesta prova». Encara no s’ha registrat cap prova amb aquest botó.`;main.querySelector('#experiment-model').scrollIntoView({behavior:'smooth',block:'start'});});
}
